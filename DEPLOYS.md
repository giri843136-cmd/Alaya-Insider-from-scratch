# Deploy log

Appended automatically by `scripts/deploy-hostinger.sh` (one row per successful
deploy — see RUNBOOK "Deploying to Hostinger"). Set Rollback=yes by hand if you
run `scripts/rollback-hostinger.sh`. Commit the appended row before the next
deploy (the worktree must be clean to deploy).

| Date (UTC)      | Sha                                      | Checks passed | Rollback |
|-----------------|------------------------------------------|---------------|----------|
